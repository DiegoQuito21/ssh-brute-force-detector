firewall.py receives an IP address
check if that IP is already in blocked list
    → YES: print "IP already blocked" and stop
    → NO: 
        print the iptables command we WOULD run
        add IP to blocked list in memory
        save it to blocked_ips.txt
        print confirmation that IP was blocked